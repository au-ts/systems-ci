[project]
name = "ts_ci"
version = "0.1"
requires-python = ">= 3.9"
readme = "README.md"

[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-dir]
ts_ci = "."
